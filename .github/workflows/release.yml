name: Release Stable APK

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Download Gradle wrapper
        run: |
          mkdir -p gradle/wrapper
          curl -fsSL "https://services.gradle.org/distributions/gradle-8.9-bin.zip" \
            -o /tmp/gradle-8.9-bin.zip
          unzip -q /tmp/gradle-8.9-bin.zip -d /tmp/gradle-dist
          /tmp/gradle-dist/gradle-8.9/bin/gradle wrapper --gradle-version 8.9 \
            --distribution-type bin
          chmod +x gradlew

      - name: Bundle Delta APK into assets
        run: |
          mkdir -p app/src/main/assets
          echo "Downloading Delta APK..."
          curl -L --retry 5 --retry-delay 3 \
            "https://delta.filenetwork.vip/file/Delta-2.736.1408-02.apk" \
            -o app/src/main/assets/roblox_delta.apk
          echo "Bundled APK size: $(du -sh app/src/main/assets/roblox_delta.apk | cut -f1)"

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Build release APK
        run: ./gradlew assembleRelease --stacktrace

      - name: Sign APK
        uses: r0adkll/sign-android-release@v1
        id: sign
        with:
          releaseDirectory: app/build/outputs/apk/release
          signingKeyBase64: ${{ secrets.SIGNING_KEY_BASE64 }}
          alias: ${{ secrets.KEY_ALIAS }}
          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}

      - name: Rename signed APK
        run: |
          mv "${{ steps.sign.outputs.signedReleaseFile }}" \
             "app/build/outputs/apk/release/Halo-Roblox-${{ steps.version.outputs.VERSION }}.apk"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "☠️🥀 Halo Roblox Launcher ${{ steps.version.outputs.VERSION }}"
          tag_name: ${{ steps.version.outputs.VERSION }}
          body: |
            ## Halo Roblox Launcher ${{ steps.version.outputs.VERSION }}

            ### ☠️ Standalone — no internet needed
            Roblox (Delta build) is bundled inside. Just install and play.

            ### Install
            1. Download APK below (~220MB)
            2. Enable **Install from unknown sources**
            3. Tap install — Roblox installs itself on first launch

            > Powered by Delta v2.736 ☠️
          files: |
            app/build/outputs/apk/release/Halo-Roblox-${{ steps.version.outputs.VERSION }}.apk
          draft: false
          prerelease: false
